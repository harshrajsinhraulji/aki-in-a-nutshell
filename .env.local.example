# ================================
# Aki's World Environment Variables
# ================================
# Copy this file to .env.local and fill in the values

# --- Vercel Blob (File Storage) ---
VERCEL_BLOB_TOKEN=
VERCEL_PROJECT=

# --- Firebase Client (Public - Safe for browser) ---
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=

# --- Firebase Admin (Server-only - Keep SECRET) ---
# This should be the full JSON string of your service account key
FIREBASE_SERVICE_ACCOUNT_KEY=

# --- Aki's Room Authentication ---
# Password hash for /aki page access (generate with bcrypt)
AKI_PASSWORD_HASH=
# Secret for signing session cookies (generate a random 32+ char string)
AKI_SESSION_SECRET=
